apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ssb-app.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ssb-app.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
